class OrderingProduct {
  constructor({id, statusId, orderingId, productId}) {
    this.id = id;
    this.statusId = statusId;
    this. orderingId = orderingId;
    this.productId = productId;
  }
}

module.exports = OrderingProduct;
